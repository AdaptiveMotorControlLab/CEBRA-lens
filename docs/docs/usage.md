# 📊 How to use?

The CEBRA-Lens package allows for analyzing the embeddings of a single model, but also offers the functionality of comparing embeddings and behavior through layers between models. For this purpose the code contains the `class 
